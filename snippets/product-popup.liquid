<div id="product-popup" class="popup-overlay" style="display: none;">
  <div class="popup-container">
    <button class="popup-close" aria-label="Close">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
    
    <div class="popup-content">
      <!-- Left Column: Product Image -->
      <div class="popup-image">
        <img id="popup-product-image" src="" alt="Product image">
      </div>
      
      <!-- Right Column: Product Details -->
      <div class="popup-details">
        <h2 id="popup-product-title" class="popup-title"></h2>
        <div class="popup-price-wrapper">
          <span id="popup-product-price" class="popup-price"></span>
          <span id="popup-regular-price" class="popup-regular-price"></span>
        </div>
        
        <div id="popup-product-description" class="popup-description"></div>
        
        <!-- Color Dropdown -->
        <div class="variant-group">
          <label class="variant-label">Color</label>
          <div class="variant-dropdown" id="color-dropdown">
            <button type="button" class="dropdown-btn" id="color-dropdown-btn">
              <span id="selected-color-text">Select Color</span>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M6 9l6 6 6-6"/>
              </svg>
            </button>
            <div class="dropdown-menu" id="color-dropdown-menu"></div>
          </div>
        </div>
        
        <!-- Size Dropdown -->
        <div class="variant-group">
          <label class="variant-label">Size</label>
          <div class="variant-dropdown" id="size-dropdown">
            <button type="button" class="dropdown-btn" id="size-dropdown-btn">
              <span id="selected-size-text">Choose your size</span>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M6 9l6 6 6-6"/>
              </svg>
            </button>
            <div class="dropdown-menu" id="size-dropdown-menu"></div>
          </div>
        </div>
        
        <!-- Quantity and Add to Cart -->
        <div class="popup-actions">
          <div class="quantity-selector">
            <button class="quantity-btn" data-action="decrease" aria-label="Decrease quantity">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="5" y1="12" x2="19" y2="12"/>
              </svg>
            </button>
            <input type="number" id="quantity-input" value="1" min="1" class="quantity-input">
            <button class="quantity-btn" data-action="increase" aria-label="Increase quantity">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="12" y1="5" x2="12" y2="19"/>
                <line x1="5" y1="12" x2="19" y2="12"/>
              </svg>
            </button>
          </div>
          <button id="add-to-cart-btn" class="add-to-cart-btn">ADD TO CART</button>
        </div>
        
        <div id="cart-message" class="cart-message"></div>
      </div>
    </div>
  </div>
</div>

<style>
/* ============================================ */
/* POPUP STYLES - PIXEL PERFECT                 */
/* ============================================ */

.popup-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(8px);
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.popup-overlay.active {
  opacity: 1;
  visibility: visible;
}

.popup-container {
  background: white;
  max-width: 1000px;
  width: 90%;
  max-height: 90vh;
  border-radius: 0;
  position: relative;
  overflow-y: auto;
  transform: scale(0.95);
  transition: transform 0.3s ease;
}

.popup-overlay.active .popup-container {
  transform: scale(1);
}

/* Custom Scrollbar */
.popup-container::-webkit-scrollbar {
  width: 6px;
}

.popup-container::-webkit-scrollbar-track {
  background: #f1f1f1;
}

.popup-container::-webkit-scrollbar-thumb {
  background: #888;
}

.popup-close {
  position: absolute;
  top: 20px;
  right: 20px;
  width: 40px;
  height: 40px;
  background: white;
  border: none;
  border-radius: 50%;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  transition: all 0.2s ease;
}

.popup-close:hover {
  transform: scale(1.05);
  background: #f5f5f5;
}

.popup-close svg {
  width: 20px;
  height: 20px;
}

/* Popup Content Layout */
.popup-content {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 48px;
  padding: 40px;
}

@media (max-width: 768px) {
  .popup-content {
    grid-template-columns: 1fr;
    gap: 24px;
    padding: 24px;
  }
  
  .popup-container {
    width: 95%;
    max-height: 95vh;
  }
}

/* Product Image */
.popup-image {
  border-radius: 0;
  overflow: hidden;
  background: #f8f8f8;
}

.popup-image img {
  width: 100%;
  height: auto;
  display: block;
  object-fit: cover;
}

/* Product Details */
.popup-details {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.popup-title {
  font-size: 24px;
  font-weight: 600;
  margin: 0;
  color: #1a1a1a;
  line-height: 1.3;
  letter-spacing: -0.02em;
}

@media (max-width: 768px) {
  .popup-title {
    font-size: 20px;
  }
}

.popup-price-wrapper {
  margin-bottom: 0;
}

.popup-price {
  font-size: 28px;
  font-weight: 600;
  color: #1a1a1a;
}

.popup-regular-price {
  font-size: 18px;
  color: #999;
  text-decoration: line-through;
  margin-left: 12px;
}

.popup-description {
  font-size: 14px;
  line-height: 1.6;
  color: #666;
  margin: 8px 0;
}

/* ============================================ */
/* DROPDOWN STYLES                              */
/* ============================================ */

.variant-group {
  margin-bottom: 20px;
}

.variant-label {
  display: block;
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 12px;
  color: #1a1a1a;
}

.variant-dropdown {
  position: relative;
  width: 100%;
}

.dropdown-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 16px;
  background: white;
  border: 1px solid #e5e5e5;
  border-radius: 0;
  cursor: pointer;
  font-size: 14px;
  color: #1a1a1a;
  transition: all 0.2s ease;
}

.dropdown-btn:hover {
  border-color: #1a1a1a;
}

.dropdown-menu {
  position: absolute;
  top: calc(100% + 4px);
  left: 0;
  right: 0;
  background: white;
  border: 1px solid #e5e5e5;
  z-index: 100;
  max-height: 200px;
  overflow-y: auto;
  opacity: 0;
  visibility: hidden;
  transform: translateY(-10px);
  transition: all 0.2s ease;
}

.dropdown-menu.active {
  opacity: 1;
  visibility: visible;
  transform: translateY(0);
}

.dropdown-option {
  padding: 12px 16px;
  cursor: pointer;
  font-size: 14px;
  transition: background 0.2s ease;
}

.dropdown-option:hover {
  background: #f5f5f5;
}

.dropdown-option.selected {
  background: #1a1a1a;
  color: white;
}

/* ============================================ */
/* QUANTITY SELECTOR & ADD TO CART             */
/* ============================================ */

.popup-actions {
  display: flex;
  gap: 16px;
  align-items: center;
  margin-top: 8px;
  flex-wrap: wrap;
}

@media (max-width: 480px) {
  .popup-actions {
    flex-direction: column;
  }
}

.quantity-selector {
  display: flex;
  align-items: center;
  border: 1px solid #e5e5e5;
  border-radius: 0;
  overflow: hidden;
}

.quantity-btn {
  width: 48px;
  height: 52px;
  background: white;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.2s ease;
}

.quantity-btn:hover {
  background: #f5f5f5;
}

.quantity-input {
  width: 60px;
  height: 52px;
  text-align: center;
  border: none;
  border-left: 1px solid #e5e5e5;
  border-right: 1px solid #e5e5e5;
  font-size: 16px;
  font-weight: 500;
}

.quantity-input:focus {
  outline: none;
}

.add-to-cart-btn {
  flex: 1;
  background: #1a1a1a;
  color: white;
  border: none;
  padding: 0 24px;
  height: 52px;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.2s ease;
}

.add-to-cart-btn:hover {
  background: #333;
  transform: translateY(-1px);
}

/* Cart Message */
.cart-message {
  margin-top: 16px;
  padding: 12px;
  text-align: center;
  font-size: 14px;
  animation: fadeInUp 0.3s ease;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.cart-message.success {
  background: #d4edda;
  color: #155724;
}

.cart-message.error {
  background: #f8d7da;
  color: #721c24;
}

/* Mobile Optimizations */
@media (max-width: 768px) {
  .dropdown-btn {
    padding: 12px 14px;
  }
  
  .quantity-btn,
  .quantity-input,
  .add-to-cart-btn {
    height: 48px;
  }
  
  .add-to-cart-btn {
    font-size: 13px;
  }
}
</style>